import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    backgroundColor: "#4F63AC",
    paddingVertical: 20,
    paddingHorizontal: 8,
    width: "100%",
    borderRadius: 10,
  },
  title: {
    color: "#FFFFFF",
    textAlign: "center",
    fontSize: 16,
    fontWeight: "bold",
  },
});
